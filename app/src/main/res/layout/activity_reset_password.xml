<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorWhite"
    android:orientation="vertical">

    <ImageView
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:layout_gravity="center"
        android:layout_marginTop="64dp"
        android:src="@drawable/ic_logo" />

    <EditText
        android:id="@+id/et_reset_password_email"
        android:layout_width="240dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:layout_marginTop="48dp"
        android:background="@color/colorSplit"
        android:gravity="center"
        android:hint="@string/ac_reset_password_email"
        android:inputType="textEmailAddress"
        android:maxLines="1"
        android:textColorHint="@color/colorTips"
        android:textSize="16sp" />

    <LinearLayout
        android:layout_width="240dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"
        android:gravity="start">

        <TextView
            android:id="@+id/tv_reset_password_cancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/ac_reset_password_cancel"
            android:textColor="@color/colorAccent"
            android:textSize="12sp" />

    </LinearLayout>

    <Button
        android:id="@+id/btn_reset_password_reset"
        android:layout_width="240dp"
        android:layout_height="56dp"
        android:layout_gravity="center"
        android:layout_marginTop="152dp"
        android:background="@drawable/ic_btn_ripple"
        android:text="@string/ac_reset_password_get_email"
        android:textColor="@color/colorWhite"
        android:textSize="16sp" />

</LinearLayout>
